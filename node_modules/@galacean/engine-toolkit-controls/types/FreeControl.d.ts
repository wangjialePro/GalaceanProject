import { Entity, InputManager, Script } from "@galacean/engine";
import { IControlInput } from "./inputDevice/IControlInput";
/**
 * The camera's roaming controller, can move up and down, left and right, and rotate the viewing angle.
 */
export declare class FreeControl extends Script {
    input: InputManager;
    inputDevices: IControlInput[];
    /** Movement distance per second, the unit is the unit before MVP conversion. */
    movementSpeed: number;
    /** Rotate speed. */
    rotateSpeed: number;
    /** Simulate a ground. */
    floorMock: boolean;
    /** Simulated ground height. */
    floorY: number;
    private _cameraTransform;
    private _spherical;
    private _tempVec;
    constructor(entity: Entity);
    onUpdate(deltaTime: number): void;
    private _pan;
    private _rotate;
}
