/**
 * The platform (including operating system and hardware) is running on.
 */
export declare enum Platform {
    /** Android platform. */
    Android = 0,
    /** IPhone platform. */
    IPhone = 1,
    /** IPad platform. */
    IPad = 2,
    /** Mac platform. */
    Mac = 3,
    /** Unknown platform. */
    Unknown = 4
}
