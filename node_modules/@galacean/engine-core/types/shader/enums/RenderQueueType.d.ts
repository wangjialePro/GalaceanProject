/**
 * Render queue type.
 */
export declare enum RenderQueueType {
    /** Opaque queue. */
    Opaque = 0,
    /** Opaque queue, alpha cutoff. */
    AlphaTest = 1,
    /** Transparent queue, rendering from back to front to ensure correct rendering of transparent objects. */
    Transparent = 2
}
