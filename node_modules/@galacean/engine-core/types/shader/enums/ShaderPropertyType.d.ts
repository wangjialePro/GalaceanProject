/**
 * Shader Property type.
 */
export declare enum ShaderPropertyType {
    /** Float type. */
    Float = 0,
    /** Int type. */
    Int = 1,
    /** Vector2 type. */
    Vector2 = 2,
    /** Vector3 type. */
    Vector3 = 3,
    /** Vector4 type. */
    Vector4 = 4,
    /** Matrix type. */
    Matrix = 5,
    /** Color type. */
    Color = 6,
    /** Texture type. */
    Texture = 7,
    /** Float array type. */
    FloatArray = 8,
    /** Int array type. */
    IntArray = 9,
    /** Texture array type. */
    TextureArray = 10
}
