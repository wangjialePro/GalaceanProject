/**
 * Fog Mode.
 */
export declare enum FogMode {
    /** Disable fog. */
    None = 0,
    /** Linear fog. */
    Linear = 1,
    /** Exponential fog. */
    Exponential = 2,
    /** Exponential squared fog. */
    ExponentialSquared = 3
}
