/**
 * Set which face for render.
 */
export declare enum RenderFace {
    /** Render front face. */
    Front = 0,
    /** Render back face. */
    Back = 1,
    /** Render double face. */
    Double = 2
}
